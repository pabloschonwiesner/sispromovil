import 'package:flutter/material.dart';

class OrdenesFinalizadas extends StatelessWidget {
  static const String routeName = '/finalizadas';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Row(children: <Widget>[Text('Ordenes Finalizadas')],),
    );
  }
}