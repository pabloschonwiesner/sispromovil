import 'package:flutter/material.dart';

class OrdenesPendientesPlanificadas extends StatelessWidget {
  static const String routeName = '/pendientesPlanificadas';

 @override
 Widget build(BuildContext context) {
  return Center(
    child: Row(children: <Widget>[Text('Ordenes pendientes planificadas')],),
  );
 }
}